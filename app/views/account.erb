<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.user_id && Helpers.is_logged_in? %>
    <h3>Hello, <%= @user.name %></h3>
    <p> Your balance is: <%= @user.balance %></p>
    <p><a href="/logout">Log Out</a></p>
    <% else %>
    <a href="/">Home</a>
    <% end %>
  </body>
</html>
